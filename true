# Ce fichier n'est plus utilisé pour la synchronisation avec le wiki.
# Le contenu est maintenant automatiquement publié par GitBook.

if __name__ == "__main__":
    print("Ce script n'est plus utilisé pour la synchronisation avec le wiki.")
    print("Le contenu est maintenant automatiquement publié par GitBook.")
    print("Veuillez utiliser les outils GitBook appropriés pour gérer le contenu.")
